import 'package:flutter/material.dart';

const double kContentSpacing = 20.0;

const Color kTextColor = Color.fromRGBO(173, 173, 173, 1);

const KtextStyle =  TextStyle(
  color: kTextColor,
  fontSize: 15
  );


const kfoodListPadding = EdgeInsets.only(top: 25, bottom: 0, left: 15, right: 15);
const kPagePadding = EdgeInsets.only(top: 25, bottom: 15, left: 22, right: 22);

const kPageHeading = TextStyle(
  fontSize: 25,
  fontWeight: FontWeight.w700
);